# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env.local
.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?


.supa
CLAUDE.md
coverage/
.claude/
supabase/.temp/

# Docker build artifacts (the .tar files are USB deliverables, not source)
shule-install/*.tar
shule-install/supabase-images/*.tar

# Never ignore these — they are source of truth
# supabase/migrations/   ← always commit
# supabase/seeds/        ← always commit
# supabase/functions/    ← always commit

# Playwright / test artefacts
test-results/
playwright-report/
e2e-results*.txt
unit-test-results.txt

# Vite PWA dev cache
dev-dist/